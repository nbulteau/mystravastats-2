# Copilot Instructions

Follow the repository agent guide in:

- `AGENTS.md` (repo root)

Additional Copilot-specific reminders:

- Prefer minimal, focused diffs.
- Keep Go and Kotlin route behavior in sync.
- Add or update tests when changing route-generation logic.
- Avoid touching unrelated local/IDE files.

